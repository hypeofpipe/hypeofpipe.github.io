export default {
    plugins: {
        '@tailwindcss/postcss': {},
        autoprefixer: {}, // autoprefixer is usually included in v4 but keeping it doesn't hurt, or I can remove it if redundant. v4 handles it. I will keep it simple.
    },
}
